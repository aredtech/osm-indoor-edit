{
  "name": "osm-indoor",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@10.0.0",
  "scripts": {
    "build": "tsc -b",
    "typecheck": "tsc --noEmit -p tsconfig.typecheck.json",
    "test": "vitest run",
    "dev:leaflet": "pnpm --filter @aredtech/osm-indoor-edit-example-leaflet dev",
    "dev:maplibre": "pnpm --filter @aredtech/osm-indoor-edit-example-maplibre dev",
    "build:examples": "pnpm --filter @aredtech/osm-indoor-edit-example-vanilla build && pnpm --filter @aredtech/osm-indoor-edit-example-leaflet build && pnpm --filter @aredtech/osm-indoor-edit-example-maplibre build",
    "check:core-boundary": "node scripts/check-core-boundary.mjs",
    "release:dry-run": "node scripts/release-dry-run.mjs",
    "test:e2e": "pnpm test:e2e:leaflet && pnpm test:e2e:maplibre",
    "test:e2e:leaflet": "playwright test tests/e2e/leaflet-example.spec.ts --project=leaflet",
    "test:e2e:maplibre": "playwright test tests/e2e/maplibre-example.spec.ts --project=maplibre"
  },
  "devDependencies": {
    "@playwright/test": "^1.60.0",
    "@types/leaflet": "^1.9.21",
    "happy-dom": "^20.0.0",
    "typescript": "^5.9.0",
    "vite": "^7.0.0",
    "vitest": "^4.0.0"
  }
}
