---
phase: 02-leaflet-editing-mvp
plan: "04"
type: execute
wave: 4
depends_on:
  - "02-01"
  - "02-02"
  - "02-03"
files_modified:
  - package.json
  - pnpm-lock.yaml
  - playwright.config.ts
  - tsconfig.typecheck.json
  - packages/core/src/levels.ts
  - packages/core/src/editor.ts
  - packages/core/src/index.ts
  - packages/core/test/levels.test.ts
  - packages/core/test/editor-levels.test.ts
  - packages/leaflet/src/leaflet-adapter.ts
  - packages/leaflet/test/leaflet-levels.test.ts
  - examples/leaflet/package.json
  - examples/leaflet/index.html
  - examples/leaflet/src/main.ts
  - examples/leaflet/src/styles.css
  - tests/e2e/leaflet-example.spec.ts
autonomous: true
requirements:
  - INDOOR-04
  - INDOOR-05
  - ADAPT-02
  - API-03
  - API-04
  - DRAW-01
  - DRAW-02
  - DRAW-03
  - DRAW-04
  - DRAW-05
  - DRAW-06
  - EDIT-01
  - EDIT-02
  - EDIT-03
  - EDIT-04
  - EDIT-05
  - EDIT-06
  - EDIT-07
  - INDOOR-01
  - INDOOR-02
  - INDOOR-03
must_haves:
  truths:
    - D-08 is implemented by committed Leaflet visual filtering on level changes and active draft rejection/cancel behavior.
    - D-16 is implemented by simple `level` and `repeat_on` visibility filtering.
    - D-18, D-19, and D-20 are implemented by adapter tests plus a bare Leaflet example and Playwright smoke.
    - UI-SPEC layout, copy, and color contracts are represented in the example and Leaflet visual defaults.
  artifacts:
    - packages/core/src/levels.ts
    - examples/leaflet/index.html
    - examples/leaflet/src/main.ts
    - tests/e2e/leaflet-example.spec.ts
    - playwright.config.ts
  key_links:
    - The final gate proves Phase 2 with root build, typecheck, unit tests, and browser smoke.
---

<objective>
Complete the Phase 2 proof: level/repeat_on filtering, bare Leaflet example with host-owned controls, Playwright smoke coverage, and final build/typecheck/test/browser gate.

Purpose: Prove the Leaflet MVP works in a browser without turning the example into a product UI.
Output: Level filtering helpers, example app, e2e smoke, and final verification.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-leaflet-editing-mvp/02-CONTEXT.md
@.planning/phases/02-leaflet-editing-mvp/02-RESEARCH.md
@.planning/phases/02-leaflet-editing-mvp/02-UI-SPEC.md
@.planning/phases/02-leaflet-editing-mvp/02-PATTERNS.md
@.planning/phases/02-leaflet-editing-mvp/02-01-SUMMARY.md
@.planning/phases/02-leaflet-editing-mvp/02-02-SUMMARY.md
@.planning/phases/02-leaflet-editing-mvp/02-03-SUMMARY.md
</context>

<threat_model>
This plan proves user-visible integration. Main threats are false confidence from unit-only tests, example scope creep, and level filtering that hides or shows wrong features.

- Threat: example becomes SDK-owned UI kit. Mitigation: UI-SPEC says host-owned controls only; acceptance checks exact copy and absence of decorative hero/shell.
- Threat: browser smoke depends on remote tiles. Mitigation: smoke checks controls/export path and map container without requiring tile success.
- Threat: `repeat_on` parsing mishandles common delimiters. Mitigation: unit tests cover comma and semicolon values.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Implement level and repeat_on visibility filtering</name>
  <files>packages/core/src/levels.ts, packages/core/src/editor.ts, packages/core/src/index.ts, packages/core/test/levels.test.ts, packages/core/test/editor-levels.test.ts, packages/leaflet/src/leaflet-adapter.ts, packages/leaflet/test/leaflet-levels.test.ts</files>
  <read_first>packages/core/src/feature-store.ts, packages/core/src/editor.ts, packages/leaflet/src/leaflet-adapter.ts, .planning/phases/02-leaflet-editing-mvp/02-CONTEXT.md</read_first>
  <action>Create `packages/core/src/levels.ts` with `parseRepeatOn(value?: string): string[]` and `isFeatureVisibleOnLevel(feature: FeatureRecord, level: string | undefined): boolean`. `parseRepeatOn` must split comma and semicolon separated values, trim whitespace, and drop empty parts. Visibility is true when no current level is set, when `feature.level` equals current level, when `feature.tags.level` equals current level, or when `feature.tags.repeat_on` includes current level. Export from `index.ts`. Update editor `setLevel` so active draft is cancelled or rejected per D-08 and committed adapter visuals are refreshed. Update Leaflet adapter with a method or internal path to hide/show committed layers based on feature visibility. Add tests for `level`, `repeat_on`, active draft on level change, and Leaflet committed layer filtering.</action>
  <verify>pnpm test -- --runInBand packages/core/test/levels.test.ts packages/core/test/editor-levels.test.ts packages/leaflet/test/leaflet-levels.test.ts</verify>
  <acceptance_criteria>
    - `packages/core/src/levels.ts` contains `parseRepeatOn`.
    - `packages/core/src/levels.ts` contains `isFeatureVisibleOnLevel`.
    - `packages/core/test/levels.test.ts` contains `repeat_on`.
    - `packages/core/test/levels.test.ts` contains `semicolon`.
    - `packages/core/test/editor-levels.test.ts` contains `active draft`.
    - `packages/leaflet/test/leaflet-levels.test.ts` contains `filters committed`.
    - `pnpm test -- --runInBand packages/core/test/levels.test.ts packages/core/test/editor-levels.test.ts packages/leaflet/test/leaflet-levels.test.ts` exits 0.
  </acceptance_criteria>
  <done>Committed visuals and editor state respect current level and simple repeat_on semantics.</done>
</task>

<task type="auto">
  <name>Task 2: Build bare Leaflet example with host-owned controls</name>
  <files>examples/leaflet/package.json, examples/leaflet/index.html, examples/leaflet/src/main.ts, examples/leaflet/src/styles.css</files>
  <read_first>examples/leaflet/package.json, examples/leaflet/src/main.ts, .planning/phases/02-leaflet-editing-mvp/02-UI-SPEC.md, .planning/phases/02-leaflet-editing-mvp/02-CONTEXT.md</read_first>
  <action>Replace the Leaflet example placeholder with a Vite browser entry. `examples/leaflet/package.json` must include scripts `"dev": "vite --host 127.0.0.1"`, `"build": "vite build"`, and dependencies on `@osminedit-lib/core`, `@osminedit-lib/leaflet`, and `leaflet` version `^1.9.4`. Add `index.html` with a `div id="app"`. In `src/main.ts`, initialize a Leaflet map, attach `createLeafletAdapter`, create an editor with default level `"0"`, render host-owned controls with exact labels `Draw room`, `Draw corridor`, `Add POI`, `Finish`, `Cancel`, `Delete feature`, `Delete vertex`, a level selector with `0`, `1`, `2`, a simple tag/name input, a status line, and an export JSON panel. Use SDK APIs for draw, finish, cancel, level, update tags, delete feature, and export. In `src/styles.css`, implement the UI-SPEC layout, color, spacing, typography, fixed control heights, scrollable export panel, and no hero/marketing layout.</action>
  <verify>pnpm --filter @osminedit-lib/example-leaflet build</verify>
  <acceptance_criteria>
    - `examples/leaflet/package.json` contains `"dev": "vite --host 127.0.0.1"`.
    - `examples/leaflet/package.json` contains `"leaflet": "^1.9.4"`.
    - `examples/leaflet/index.html` contains `id="app"`.
    - `examples/leaflet/src/main.ts` contains `Draw room`.
    - `examples/leaflet/src/main.ts` contains `Draw corridor`.
    - `examples/leaflet/src/main.ts` contains `Add POI`.
    - `examples/leaflet/src/main.ts` contains `status: true`.
    - `examples/leaflet/src/styles.css` contains `#2563EB`.
    - `examples/leaflet/src/styles.css` does not contain `hero`.
    - `pnpm --filter @osminedit-lib/example-leaflet build` exits 0.
  </acceptance_criteria>
  <done>Bare Leaflet example proves host-owned controls drive SDK map editing/export.</done>
</task>

<task type="auto">
  <name>Task 3: Add Playwright smoke test for Leaflet example</name>
  <files>package.json, pnpm-lock.yaml, playwright.config.ts, tsconfig.typecheck.json, tests/e2e/leaflet-example.spec.ts</files>
  <read_first>package.json, examples/leaflet/package.json, examples/leaflet/src/main.ts, .planning/phases/02-leaflet-editing-mvp/02-RESEARCH.md</read_first>
  <action>Add `@playwright/test` as a root dev dependency and root script `"test:e2e": "playwright test"`. Create `playwright.config.ts` with `webServer` command `pnpm --filter @osminedit-lib/example-leaflet dev`, url `http://127.0.0.1:5173`, and `reuseExistingServer: !process.env.CI`. Update `tsconfig.typecheck.json` so the typecheck includes `tests/**/*.ts` if it does not already. Create `tests/e2e/leaflet-example.spec.ts` that opens `/`, asserts host controls `Draw room`, `Draw corridor`, `Add POI`, `Finish`, `Cancel`, level selector, and export panel exist, then verifies export panel initially includes `"status": true`. Add a smoke interaction path that changes level to `1` and verifies visible status text updates. Do not rely on remote map tiles loading.</action>
  <verify>pnpm test:e2e</verify>
  <acceptance_criteria>
    - `package.json` contains `"test:e2e": "playwright test"`.
    - `package.json` contains `"@playwright/test"`.
    - `playwright.config.ts` contains `webServer`.
    - `playwright.config.ts` contains `pnpm --filter @osminedit-lib/example-leaflet dev`.
    - `tsconfig.typecheck.json` contains `tests/**/*.ts`.
    - `tests/e2e/leaflet-example.spec.ts` contains `Draw room`.
    - `tests/e2e/leaflet-example.spec.ts` contains `status`.
    - `pnpm test:e2e` exits 0.
  </acceptance_criteria>
  <done>Browser smoke proves the bare Leaflet example loads and exposes the export path.</done>
</task>

<task type="auto">
  <name>Task 4: Run final Phase 2 verification gate</name>
  <files>package.json, pnpm-lock.yaml, packages/core/test/**/*.test.ts, packages/leaflet/test/**/*.test.ts, examples/leaflet/src/main.ts, tests/e2e/leaflet-example.spec.ts</files>
  <read_first>package.json, pnpm-workspace.yaml, tsconfig.json, vitest.config.ts, playwright.config.ts, .planning/phases/02-leaflet-editing-mvp/02-CONTEXT.md, .planning/phases/02-leaflet-editing-mvp/02-UI-SPEC.md</read_first>
  <action>Run the full Phase 2 gate and fix failures in Phase 2 scope only: `pnpm build`, `pnpm typecheck`, `pnpm test`, `pnpm --filter @osminedit-lib/example-leaflet build`, and `pnpm test:e2e`. Confirm verbose test output includes Leaflet adapter/editing/levels tests and Playwright smoke. Do not add MapLibre behavior, shared-node snapping, robust validation, or polished documentation in this phase.</action>
  <verify>pnpm build && pnpm typecheck && pnpm test && pnpm --filter @osminedit-lib/example-leaflet build && pnpm test:e2e</verify>
  <acceptance_criteria>
    - `pnpm build` exits 0.
    - `pnpm typecheck` exits 0.
    - `pnpm test` exits 0.
    - `pnpm --filter @osminedit-lib/example-leaflet build` exits 0.
    - `pnpm test:e2e` exits 0.
    - test output or verbose rerun includes `leaflet-adapter.test.ts`.
    - test output or verbose rerun includes `leaflet-editing.test.ts`.
    - test output or verbose rerun includes `leaflet-example.spec.ts`.
  </acceptance_criteria>
  <done>Phase 2 Leaflet MVP proof gate passes.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm build` exits 0.
- [ ] `pnpm typecheck` exits 0.
- [ ] `pnpm test` exits 0.
- [ ] `pnpm --filter @osminedit-lib/example-leaflet build` exits 0.
- [ ] `pnpm test:e2e` exits 0.
- [ ] `rg "Draw room|Draw corridor|Add POI|status: true" examples/leaflet/src tests/e2e` finds example proof.
- [ ] `rg "repeat_on|isFeatureVisibleOnLevel" packages/core/src packages/core/test` finds level filtering.
</verification>

<success_criteria>

- Current-level and repeat_on filtering work.
- Bare Leaflet example demonstrates host-owned controls driving draw/edit/export.
- Browser smoke proves the example loads without relying on tile success.
- Final Phase 2 gate passes.
- No MapLibre, snapping, robust validation, polished docs, or SDK-owned app UI are added.
</success_criteria>

<output>
After completion, create `.planning/phases/02-leaflet-editing-mvp/02-04-SUMMARY.md`
</output>
