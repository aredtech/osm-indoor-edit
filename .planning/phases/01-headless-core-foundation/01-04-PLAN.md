---
phase: 01-headless-core-foundation
plan: "04"
type: execute
wave: 4
depends_on:
  - "01-01"
  - "01-02"
  - "01-03"
files_modified:
  - packages/core/src/import-export.ts
  - packages/core/src/fake-adapter.ts
  - packages/core/src/index.ts
  - packages/core/test/fixtures/sample-room.json
  - packages/core/test/fixtures/sample-relation.json
  - packages/core/test/fixtures/imported-custom-way.json
  - packages/core/test/import-export.test.ts
  - packages/core/test/fake-adapter.test.ts
  - packages/core/test/snapshots/osminedit-export.snap.json
autonomous: true
requirements:
  - IO-03
  - ADAPT-04
user_setup: []
must_haves:
  truths:
    - D-18 is implemented by proving Phase 1 through automated tests and fixture round-trips only.
    - D-19 is implemented by fixtures for sample room, sample relation, and imported custom way.
    - D-20 is implemented by the final verification gate: build, typecheck, unit tests, and fixture snapshot tests.
    - D-21 is implemented by fake adapter contract tests.
    - IO-03 is implemented by loading OsmInEdit-style JSON containing nodes, ways, and relations.
  artifacts:
    - packages/core/src/import-export.ts
    - packages/core/src/fake-adapter.ts
    - packages/core/test/fixtures/sample-room.json
    - packages/core/test/fixtures/sample-relation.json
    - packages/core/test/fixtures/imported-custom-way.json
    - packages/core/test/import-export.test.ts
    - packages/core/test/fake-adapter.test.ts
  key_links:
    - Fixture round-trip tests use `createEditor().loadOsmInEdit()` and `exportOsmInEdit()`.
    - Fake adapter tests use the core `RendererAdapter` contract without Leaflet or MapLibre.
---

<objective>
Complete Phase 1 proof through import/export fixture round-trips, snapshot tests, and fake adapter contract tests.

Purpose: Prove the headless core works without demos or real renderer packages.
Output: Required fixtures, import/export test helpers, snapshot tests, fake adapter implementation, and final verification checks.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-headless-core-foundation/01-CONTEXT.md
@.planning/phases/01-headless-core-foundation/01-RESEARCH.md
@.planning/phases/01-headless-core-foundation/01-PATTERNS.md
@.planning/phases/01-headless-core-foundation/01-01-SUMMARY.md
@.planning/phases/01-headless-core-foundation/01-02-SUMMARY.md
@.planning/phases/01-headless-core-foundation/01-03-SUMMARY.md
</context>

<threat_model>
This plan verifies correctness. Main risks are false confidence from shallow fixtures and fake adapter tests that do not exercise the contract.

- Threat: fixture tests only cover happy-path room export. Mitigation: include sample room, relation, and ambiguous custom-way fixtures.
- Threat: snapshots become nondeterministic. Mitigation: tests use injectable ID allocator and fixed clock.
- Threat: fake adapter diverges from adapter contract. Mitigation: fake adapter implements the exported `RendererAdapter` interface and tests assert attach/detach/call recording.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Add required fixtures and import/export helpers</name>
  <files>packages/core/src/import-export.ts, packages/core/src/index.ts, packages/core/test/fixtures/sample-room.json, packages/core/test/fixtures/sample-relation.json, packages/core/test/fixtures/imported-custom-way.json</files>
  <read_first>packages/core/src/types.ts, packages/core/src/primitive-store.ts, packages/core/src/feature-store.ts, packages/core/src/editor.ts, docs/osm-indoor-prd.md, .planning/phases/01-headless-core-foundation/01-CONTEXT.md</read_first>
  <action>Create `import-export.ts` helpers if editor/primitive store import/export needs a dedicated module. Add three JSON fixtures. `sample-room.json` must include at least four nodes and one closed way with first node repeated at the end, `indoor=room`, `level=0`, and `status: true`. `sample-relation.json` must include at least one relation with `members` referencing a way. `imported-custom-way.json` must include a way with valid node references but tags that do not infer `room` or `corridor`, so the feature kind falls back to `custom`. Export import/export helpers from `index.ts` only if they are public or needed by tests.</action>
  <verify>test -f packages/core/test/fixtures/sample-room.json && test -f packages/core/test/fixtures/sample-relation.json && test -f packages/core/test/fixtures/imported-custom-way.json</verify>
  <acceptance_criteria>
    - `packages/core/test/fixtures/sample-room.json` contains `"status": true`.
    - `packages/core/test/fixtures/sample-room.json` contains `"indoor": "room"`.
    - `packages/core/test/fixtures/sample-relation.json` contains `"type": "relation"`.
    - `packages/core/test/fixtures/sample-relation.json` contains `"members"`.
    - `packages/core/test/fixtures/imported-custom-way.json` contains `"type": "way"`.
  </acceptance_criteria>
  <done>Required fixtures exist and cover room, relation, and ambiguous custom import cases.</done>
</task>

<task type="auto">
  <name>Task 2: Add fixture round-trip and snapshot tests</name>
  <files>packages/core/test/import-export.test.ts, packages/core/test/snapshots/osminedit-export.snap.json</files>
  <read_first>packages/core/src/editor.ts, packages/core/src/import-export.ts, packages/core/test/fixtures/sample-room.json, packages/core/test/fixtures/sample-relation.json, packages/core/test/fixtures/imported-custom-way.json, .planning/phases/01-headless-core-foundation/01-CONTEXT.md</read_first>
  <action>Create tests that load each fixture through `createEditor().loadOsmInEdit(...)`, export through `exportOsmInEdit()`, and assert strict `{ elements, status: true }` shape. Tests must assert nodes appear before ways and relations, imported timestamps/IDs are preserved, closed way first/last node IDs match, relation members survive, and ambiguous imported way creates a `custom` feature. Add a deterministic snapshot file named `packages/core/test/snapshots/osminedit-export.snap.json` for the normalized room/relation/custom export.</action>
  <verify>pnpm test -- --runInBand packages/core/test/import-export.test.ts</verify>
  <acceptance_criteria>
    - `packages/core/test/import-export.test.ts` contains `sample-room.json`.
    - `packages/core/test/import-export.test.ts` contains `sample-relation.json`.
    - `packages/core/test/import-export.test.ts` contains `imported-custom-way.json`.
    - `packages/core/test/import-export.test.ts` contains `nodes before ways`.
    - `packages/core/test/import-export.test.ts` contains `custom`.
    - `packages/core/test/snapshots/osminedit-export.snap.json` contains `"status": true`.
  </acceptance_criteria>
  <done>Fixture round-trips and snapshots prove strict import/export behavior.</done>
</task>

<task type="auto">
  <name>Task 3: Add fake adapter contract implementation and tests</name>
  <files>packages/core/src/fake-adapter.ts, packages/core/src/index.ts, packages/core/test/fake-adapter.test.ts</files>
  <read_first>packages/core/src/adapter.ts, packages/core/src/editor.ts, .planning/phases/01-headless-core-foundation/01-CONTEXT.md</read_first>
  <action>Create `FakeRendererAdapter` in core test/support or public test utility form. It must implement `RendererAdapter`, record calls for attach/detach/layer update methods, expose a way for tests to emit map-neutral events, and avoid importing Leaflet or MapLibre. Export it only if it is intended as a testing utility; otherwise keep it internal to tests. Add tests proving it satisfies `RendererAdapter`, attach/detach calls are recorded, and editor creation can accept/use the fake adapter boundary without renderer packages.</action>
  <verify>pnpm test -- --runInBand packages/core/test/fake-adapter.test.ts</verify>
  <acceptance_criteria>
    - `packages/core/src/fake-adapter.ts` or `packages/core/test/fake-adapter.test.ts` contains `FakeRendererAdapter`.
    - `packages/core/test/fake-adapter.test.ts` contains `RendererAdapter`.
    - `packages/core/test/fake-adapter.test.ts` contains `attach`.
    - `packages/core/test/fake-adapter.test.ts` contains `detach`.
    - `packages/core/src/fake-adapter.ts` does not contain `Leaflet`.
    - `packages/core/src/fake-adapter.ts` does not contain `MapLibre`.
  </acceptance_criteria>
  <done>Fake adapter contract tests prove the renderer boundary without real map packages.</done>
</task>

<task type="auto">
  <name>Task 4: Run final Phase 1 verification gate</name>
  <files>package.json, packages/core/test/**/*.test.ts</files>
  <read_first>package.json, pnpm-workspace.yaml, tsconfig.json, vitest.config.ts, .planning/phases/01-headless-core-foundation/01-CONTEXT.md</read_first>
  <action>Run the Phase 1 gate exactly: root build, typecheck, unit tests, and fixture snapshot tests. If dependencies are missing, run `pnpm install` first. Fix failures in Phase 1 files only. Do not add browser tests, demos, Leaflet behavior, or MapLibre behavior.</action>
  <verify>pnpm build && pnpm typecheck && pnpm test</verify>
  <acceptance_criteria>
    - `pnpm build` exits 0.
    - `pnpm typecheck` exits 0.
    - `pnpm test` exits 0.
    - `pnpm test` output includes `import-export.test.ts`.
    - `pnpm test` output includes `fake-adapter.test.ts`.
  </acceptance_criteria>
  <done>Phase 1 headless proof gate passes.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm build` exits 0.
- [ ] `pnpm typecheck` exits 0.
- [ ] `pnpm test` exits 0.
- [ ] `rg "sample-room.json|sample-relation.json|imported-custom-way.json" packages/core/test` finds all required fixtures in tests.
- [ ] `rg "FakeRendererAdapter" packages/core` finds fake adapter contract tests.
</verification>

<success_criteria>

- Required fixtures exist.
- Import/export round-trip snapshots prove strict OsmInEdit-style output.
- Fake adapter contract tests prove core renderer boundary.
- Final Phase 1 verification gate passes without adding demos or real renderer behavior.
</success_criteria>

<output>
After completion, create `.planning/phases/01-headless-core-foundation/01-04-SUMMARY.md`
</output>
